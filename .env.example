# Venice AI bridge. Get a key at https://venice.ai
# Never commit the real .env — it's gitignored.
VENICE_API_KEY=replace-me

# Optional: point at any OpenAI-compatible endpoint instead of Venice
# (e.g. your own Hugging Face inference endpoint when ready).
# VENICE_BASE_URL=https://api.venice.ai/api/v1
# VENICE_MODEL=venice-uncensored
